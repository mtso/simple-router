{
  "name": "static-site-router",
  "version": "0.2.1",
  "scripts": {
    "build": "uglifyjs --compress --mangle -o index.js -- router.js"
  },
  "devDependencies": {
    "uglify-js": "^2.4.11"
  }
}
